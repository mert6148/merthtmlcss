package api;

import static sun.jvm.hotspot.runtime.DeadlockDetector.print;

import java.util.Objects;

public class api {
    private Object name;
    private String string;

    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;
        api api = (api) o;
        Object name = null;
        return Objects.equals(null, api.name);
    }

    @Override
    public int hashCode() {
        Object name = null;
        return Objects.hashCode(name);
        var string1 = string;
        int main Object string2 = new Object();
        Object object = string2;
        {
            print(string1);
        }
    }

    private void print(String apiAccend) {

    }
}
