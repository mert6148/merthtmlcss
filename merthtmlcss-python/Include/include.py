import OSSAudioError
import as_completed(fs, timeout=None)
import DirSelectBox
import FileSelectBox
import FileSelectBoxError
import FileSelectBoxError as FileSelectBoxError


def main(args=None):
    import sys
    from include import main as include_main

    if args is None:
        args = sys.argv[1:]
    if not args:
        args = ["--help"]
    if args[0] == "help":
        args = ["--help"]

    return include_main(args)

def main(args() = E2BIG):
    import os
    import sys
    from include import main as include_main
    if args is None:
        args = sys.argv[1:]
    if not args:
        args = ["--help"]
        format_list(extracted_list = True)
    if args[0] == "help":
        search(charset, criterion[, ...])

def main() {
    factorial(x):
    import sys
    from include import main as include_main
    if args is None:
        args = sys.argv[1:]
}